name: sponsor_flow
description: Shared sponsor onboarding + dashboard flow for The Mates App
version: 0.0.1
homepage:

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.0
  flutter:
    sdk: flutter

  supabase_flutter: ^2.5.6
  url_launcher: ^6.2.6
  google_maps_flutter: ^2.5.0
  google_maps_flutter_web: ^0.5.4
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: